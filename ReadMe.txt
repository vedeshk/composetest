1. Command to run docker compose. "sudo docker-compose up"
2. Command to run docker compose in detached mode. "sudo docker-compose up -d"
3. Command to stop docker compose. "sudo docker-compose stop"
4. Command to remove the images and docker volumes completely. "sudo docker-compose down --volumes"


Endpoint for the application.
localhost:5000
